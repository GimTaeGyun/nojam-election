/** @type {import('next').NextConfig} */
const nextConfig = {
  // Vercel/Netlify에 그대로 배포 가능.
  // 정적 호스팅(예: GitHub Pages, S3)에 쓰려면 아래 주석 해제.
  // output: "export",
  reactStrictMode: true,
};

export default nextConfig;
